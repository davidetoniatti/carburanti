version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - OHMYPIENO_API_URL=https://carburanti.mise.gov.it/ospzApi
    restart: unless-stopped
